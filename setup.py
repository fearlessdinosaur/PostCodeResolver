from setuptools import find_packages, setup

setup(
    name='postCodeResolver',
    packages=find_packages(include='source'),
    author='David Fox',
    version='0.1.0',
    description='A post code validator and formatter for UK postcodes'
)
